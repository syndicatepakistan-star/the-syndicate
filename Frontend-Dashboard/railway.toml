# Root directory: `Frontend-Dashboard/`. Dockerfile avoids BuildKit .next/cache mount issues.
# Railway: leave "Custom Start Command" empty in the UI — this file sets it automatically.

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "node server.js"
healthcheckPath = "/"
healthcheckTimeout = 120
