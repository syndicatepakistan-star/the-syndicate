/**
 * Side-effect import for public marketing responsive CSS.
 * Kept out of globals.css so /dashboard and auth routes do not pay for it.
 */
import "@/styles/public-marketing-responsive.css";

export function PublicMarketingStyles() {
  return null;
}
